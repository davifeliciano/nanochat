syntax = "proto3";

package chat;

message Message {
  string sender_id = 1;
  string recipient_id = 2;
  string content = 3;
  int64 created_at = 4;
}

enum EventType {
  PING = 0;
  PONG = 1;
}

message Event {
  EventType type = 1;
  string sender_id = 2;
  string recipient_id = 3;
}
